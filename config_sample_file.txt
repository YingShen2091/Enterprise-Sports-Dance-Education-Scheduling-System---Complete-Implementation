# Sports Dance Education Scheduling System Configuration
# Copy this file to config.yaml and modify as needed

# Dataset Configuration
dataset:
  num_classes: 50
  num_instructors: 10
  num_students: 200
  num_timeslots: 30
  num_venues: 5
  num_days: 5
  slots_per_day: 6
  semester_weeks: 16
  train_test_split: 0.8
  validation_split: 0.1
  batch_size: 32
  shuffle: true
  num_workers: 4
  pin_memory: true
  drop_last: false
  prefetch_factor: 2
  persistent_workers: true

# Model Configuration
model:
  architecture: "hybrid_rnn_rl"  # Options: rnn, lstm, gru, transformer, hybrid_rnn_rl, ensemble
  input_size: 1500
  hidden_size: 256
  num_layers: 3
  output_size: 1500
  dropout_rate: 0.3
  bidirectional: true
  attention_heads: 8
  feedforward_dim: 1024
  use_layer_norm: true
  use_batch_norm: false
  activation_function: "relu"
  weight_initialization: "xavier_uniform"
  gradient_clipping: 1.0
  use_residual_connections: true

# Training Configuration
training:
  num_epochs: 100
  learning_rate: 0.001
  weight_decay: 0.0001
  optimizer: "adamw"  # Options: adam, adamw, sgd, rmsprop, adagrad, adadelta
  scheduler_type: "cosine_annealing"  # Options: cosine_annealing, reduce_on_plateau, one_cycle
  warmup_epochs: 5
  early_stopping_patience: 10
  early_stopping_delta: 0.0001
  gradient_accumulation_steps: 1
  mixed_precision: true
  compile_model: false
  use_gradient_checkpointing: false
  max_grad_norm: 1.0
  
  # Loss weights for multi-objective optimization
  loss_lambda_conflict: 0.5
  loss_lambda_workload: 0.3
  loss_lambda_resource: 0.2
  loss_lambda_endurance: 0.1
  loss_lambda_continuity: 0.15

# Reinforcement Learning Configuration
reinforcement_learning:
  state_dimension: 1500
  action_dimension: 1500
  gamma: 0.95
  epsilon_start: 1.0
  epsilon_end: 0.01
  epsilon_decay: 0.995
  buffer_size: 100000
  min_buffer_size: 1000
  target_update_frequency: 10
  use_double_dqn: true
  use_prioritized_replay: true
  alpha_prioritized_replay: 0.6
  beta_prioritized_replay: 0.4
  reward_scaling: 1.0
  entropy_coefficient: 0.01

# System Configuration
system:
  device: "auto"  # Options: auto, cuda, cpu
  random_seed: 42
  num_threads: 8
  use_distributed: false
  master_addr: "localhost"
  master_port: "12355"
  world_size: 1
  rank: 0

# Database Configuration
database:
  enabled: true
  path: "scheduling_system.db"
  pool_size: 10
  cleanup_days: 90
  backup_enabled: true
  backup_interval_hours: 24

# Logging Configuration
logging:
  level: "INFO"  # Options: DEBUG, INFO, WARNING, ERROR, CRITICAL
  file: "scheduling_system.log"
  log_interval: 10
  enable_tensorboard: false
  tensorboard_log_dir: "runs"
  enable_wandb: false
  wandb_project: "sports-dance-scheduling"
  wandb_entity: null

# Checkpointing Configuration
checkpointing:
  enabled: true
  interval: 5
  directory: "checkpoints"
  keep_last: 5
  save_best: true

# Performance Configuration
performance:
  enable_profiling: false
  enable_memory_optimization: true
  cache_size_mb: 1024
  max_workers: 10

# API Configuration
api:
  enabled: false
  host: "0.0.0.0"
  port: 8000
  workers: 4
  reload: false
  cors_enabled: true
  cors_origins: ["*"]
  rate_limit_enabled: true
  rate_limit_requests: 100
  rate_limit_period: 60

# Monitoring Configuration
monitoring:
  enabled: true
  metrics_interval_seconds: 60
  system_stats_enabled: true
  performance_tracking: true
  alert_thresholds:
    cpu_percent: 90
    memory_percent: 85
    disk_percent: 90

# Optimization Constraints
constraints:
  max_consecutive_classes_per_instructor: 3
  min_break_between_classes_minutes: 15
  max_daily_hours_per_instructor: 8
  max_weekly_hours_per_instructor: 40
  max_students_per_class: 30
  min_students_per_class: 5
  max_intensity_per_day: 25
  preferred_start_times: ["08:00", "09:00", "10:00", "13:00", "14:00", "15:00"]

# Export Configuration
export:
  onnx_enabled: false
  onnx_opset_version: 14
  quantization_enabled: false
  optimization_level: 2  # 0: None, 1: Basic, 2: Extended

# Notification Configuration
notifications:
  enabled: false
  email_enabled: false
  smtp_host: "smtp.gmail.com"
  smtp_port: 587
  smtp_user: ""
  smtp_password: ""
  recipient_emails: []
  
# Development Configuration
development:
  debug_mode: false
  verbose_logging: false
  save_intermediate_results: false
  enable_assertions: false
  test_mode: false